import random

def coletar():
    return {
        "ndvi": round(random.uniform(0.3, 0.9), 2),
        "temperatura": random.randint(20, 90),
        "armazenamento": random.randint(10, 100),
        "estabilidade": random.randint(70, 100)
    }
    import random

def coletar():
    return {
        "ndvi": round(random.uniform(0.3, 0.9), 2),
        "temperatura": random.randint(20, 90),
        "armazenamento": random.randint(10, 100),
        "estabilidade": random.randint(70, 100)
    }
